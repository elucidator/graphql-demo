graphql:
  federation:
    enabled: true
  packages:
    - "nl.elucidator.graphql.demo"

server:
  port: 8083

spring:
  application:
    name: service-book
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password: password
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
